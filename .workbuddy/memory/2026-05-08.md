# 2026-05-08

## Phase 5: 同步原语完成

### 完成内容

实现了 ROADMAP Milestone 5（同步原语）：

- `AsyncEvent` — set/clear/wait，头文件已有实现
- `AsyncLock` — acquire/release/RAII LockGuard，头文件已有实现
- `AsyncSemaphore` — 计数信号量，头文件已有实现
- `AsyncBoundedSemaphore` — 有界信号量，新增于 semaphore.h，Release 超过初始值抛 InvalidStateError
- `AsyncCondition` — 条件变量，新增 Acquire/Release/Locked 代理方法
- `AsyncBarrier` — 屏障，新增 Reset() 方法（会 abort 正在等待的 waiter）
- `AsyncQueue<T>` — 有界/无界队列，支持 put/get/shutdown/join

### 修复的 bug

`AsyncConditionTest.NotifyWakesSecondWaiter` 崩溃（UBSan + ASan）：
- **根因**：`ConditionAllDoneTask(Task<void>& t1, Task<void>& t2)` 接受引用参数，而 `t1`/`t2` 是 `CallSoon` lambda 内的局部变量，lambda 执行完毕后离开作用域，协程帧里的引用悬空
- **修复**：改为 `ConditionAllDoneTask(Task<void> t1, Task<void> t2)` 按值传递
- **教训**：C++ 协程函数参数若是引用，调用者必须保证在协程帧销毁前对象存活。`Task<T>` 是共享状态的值类型，按值传递既安全又正确

### 测试结果

161/161 全部通过（新增 6 个测试：BoundedSemaphore ×3，Condition::AcquireReleaseProxy ×1，Barrier::Reset ×2）

### 文件修改

- `include/asyncio/sync/semaphore.h` — 新增 AsyncBoundedSemaphore
- `include/asyncio/sync/condition.h` — 新增 Acquire/Release/Locked 代理方法
- `include/asyncio/sync/barrier.h` — 新增 Reset() 方法
- `tests/sync/condition_test.cc` — 修复悬空引用 bug + 新增 AcquireReleaseProxy 测试
- `tests/sync/semaphore_test.cc` — 新增 BoundedSemaphore 测试
- `tests/sync/barrier_test.cc` — 新增 Reset 测试
- `TODO.md` — 标记同步原语里程碑为已完成
